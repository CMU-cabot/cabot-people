Cython
filterpy==1.4.5
matplotlib==3.6.3
numpy==1.24.1
scikit-learn==1.2.1
scipy==1.10.0
pandas==1.5.3
transforms3d==0.4.1

#Pytorch: provided by the base image (CUDA 12.2 / torch 2.1.1 on JetPack 6.0,
#CUDA 12.6 / torch 2.6.0 on JetPack 6.2)
#torch
#torchvision
#torchaudio

# AgentFormer
tensorboard
glob2
#six
#pyyaml
easydict

# SocialGAN
attrdict
#Pillow
# crowd_attn_rl.py imports gym for gym.spaces.Box / gym.spaces.Dict only.
# Pinned to what the JetPack 5 image actually ran: openai/baselines used to
# constrain this to 'gym>=0.15.4, <0.16.0' and it resolved to 0.15.7. baselines
# is no longer installed, so without this pin it would jump to 0.26.x, a
# different API generation than this code was validated against.
# 0.15.7 does not use np.bool8; the only numpy 1.24 removal it touches is np.bool
# in gym/envs/toy_text/cliffwalking.py, which `import gym` does not load (envs are
# registered by entry-point string and imported lazily).
gym==0.15.7
# tensorflow was only here to satisfy openai/baselines' setup.py, which asserts a
# tensorflow >= 1.4 is installed. baselines is no longer installed and nothing in
# the image imports tensorflow, so it is dropped. In the JetPack 5 image it
# resolved to the CPU build tensorflow-cpu-aws 2.13.1 and took 990MB.
# NOTE: do not add opencv-python here. Both the amd64 and the arm64 image build
# OpenCV from source with WITH_CUDA=ON / OPENCV_DNN_CUDA=ON and the python3
# bindings (4.9.0 on JetPack 6.0, 4.10.0 on JetPack 6.2, see docker-bake.hcl),
# installed into the very same /usr/local/lib/python3.10/dist-packages the pip
# wheel would overwrite. That silently disables the CUDA backend in
# track_people_py/scripts/detect_darknet_opencv_people.py and leaves the python
# bindings several minor versions behind the C++ libraries track_people_cpp
# links against. lidar_process itself does not import cv2.
